# Database connection
DATABASE_URL=postgresql://user:password@localhost:5444/auth

# BetterAuth configuration
BETTER_AUTH_SECRET=your-secret-key-at-least-32-characters-long
BETTER_AUTH_URL=http://localhost:3000

# Cookie configuration - use parent domain for cross-subdomain auth
COOKIE_DOMAIN=localhost

# Feature flags
ENABLE_REGISTRATION=false

# Admin configuration (comma-separated list of admin emails)
# ADMIN_EMAILS=admin@example.com,admin2@example.com

VITE_WALLETCONNECT_PROJECT_ID=project-id-from-reown